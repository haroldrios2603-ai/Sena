/**
 * Instancia de contexto React para compartir autenticación en la aplicación.
 */
import { createContext } from 'react';
import type { AuthContextType } from './types';

/**
 * Constante AuthContext utilizada en la configuración o la lógica de context.
 */
/**
 * Constante AuthContext utilizada en la configuración o la lógica de context.
 */
/**
 * Constante AuthContext utilizada en la configuración o la lógica de context.
 */
export const AuthContext = createContext<AuthContextType | undefined>(undefined);
