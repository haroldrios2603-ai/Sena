/**
 * Data Transfer Object que define la forma y validaciones del payload para dto.
 */
import { IsBoolean } from 'class-validator';

/**
 * DTO para activar o desactivar usuarios operativos.
 */
/**
 * Clase UpdateUserStatusDto que implementa la lógica principal de dto.
 */
/**
 * Clase UpdateUserStatusDto que implementa la lógica principal de dto.
 */
/**
 * Clase UpdateUserStatusDto que implementa la lógica principal de dto.
 */
export class UpdateUserStatusDto {
  /**
   * Bandera que define si la cuenta permanece activa.
   */
  @IsBoolean()
  isActive: boolean;
}
